{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: start-build",
      "request": "launch",
      "command": "yarn run start-build",
      "cwd": "${workspaceFolder}"
    }
  ]
}