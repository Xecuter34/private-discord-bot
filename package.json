{
  "name": "private-discord-bot",
  "version": "1.0.0",
  "description": "private-discord-bot",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-build": "yarn build && yarn start",
    "start-dev": "yarn node --trace-deprecation dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.5",
    "dotenv": "^10.0.0",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "discord.js": "^13.5.0"
  }
}
